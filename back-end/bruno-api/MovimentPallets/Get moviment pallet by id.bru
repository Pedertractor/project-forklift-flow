meta {
  name: Get moviment pallet by id
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/moviment-pallets/{{movimentPalletId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  # Requer JWT: roles LEADER, SUPPLY_OPERATOR ou ADMIN.
  # Parametro: movimentPalletId no environment.
}
