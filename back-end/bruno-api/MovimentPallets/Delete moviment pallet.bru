meta {
  name: Delete moviment pallet
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/moviment-pallets/{{movimentPalletId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  # Requer JWT: roles LEADER, SUPPLY_OPERATOR ou ADMIN.
  # So permite excluir sem tarefas vinculadas e sem operador no equipamento.
}
