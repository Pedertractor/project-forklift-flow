meta {
  name: MovimentPallets
  seq: 8
}

docs {
  # Prefixo: {{baseUrl}}/moviment-pallets
  # Roles: LEADER, SUPPLY_OPERATOR ou ADMIN.
  # CRUD de equipamentos de movimentacao (empilhadeira/transpaleteiro) por setor/tipo.
  # Operador de reposicao vincula-se via pasta OperatorMovimentPallet (Bind my moviment pallet).
}
