meta {
  name: Reset user password
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/users/{{userId}}/reset-password
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  # Requer JWT com role ADMIN.
  # Defina vars.userId no environment local.
}
