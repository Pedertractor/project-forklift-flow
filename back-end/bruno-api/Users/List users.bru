meta {
  name: List users
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/users
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  # Requer JWT com role ADMIN.
  # Retorna { users: [...] }; cada usuario inclui sectorId e sector (objeto ou null):
  # sector: { id, typeSector, createdAt, updatedAt } quando vinculado a um setor.
}
