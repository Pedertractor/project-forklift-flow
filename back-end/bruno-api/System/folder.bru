meta {
  name: System
  seq: 3
}

docs {
  # GET {{baseUrl}}/health — sem auth (baseUrl ja inclui /api no environment padrao).
  # POST {{baseUrl}}/orion/module-access — registra acesso dash/TV no Orion (ADMIN/LEADER).
}
