meta {
  name: OperatorMachine
  seq: 7
}

docs {
  # Prefixo: {{baseUrl}}/operator-machine
  # Roles: OPERATOR_MACHINE ou ADMIN.
  # GET/POST/DELETE my-machine; GET machines; GET replenishment-requests;
  # POST replenishment-requests/:requestId/pickup (retirada ON_MACHINE).
}
