meta {
  name: Get me
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/auth/me
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  baseUrl: http://127.0.0.1:3000/api
}

docs {
  # Requer JWT.
  # Retorna dados do usuario autenticado e firstAccess.
}
