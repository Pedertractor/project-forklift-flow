meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/auth/login
  body: json
  auth: none
}

body:json {
  {
    "card": "2282",
    "unit": "TRACTOR",
    "password": "123"
  }
  
}

vars:pre-request {
  baseUrl: http://127.0.0.1:3131/api
}

docs {
  # Publica.
  # unit: PEDERTRACTOR | TRACTOR
  # Copie o token retornado para vars.token no environment local.
}
