meta {
  name: List sectors
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/sectors
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  # Requer JWT: roles LEADER, SUPPLY_OPERATOR ou ADMIN (mesmo escopo de maquinas).
  # Retorna { sectors: [{ id, typeSector, createdAt, updatedAt }] }.
}
