meta {
  name: Get machine by id
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/machines/{{machineId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  # Requer JWT: leitura para LEADER, SUPPLY_OPERATOR, ADMIN, SUPERVISOR, MANAGER.
  # Parametro de path: machineId (variavel machineId no environment).
}
