meta {
  name: List machines
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/machines
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  # Requer JWT: roles LEADER, SUPPLY_OPERATOR ou ADMIN.
  # Query opcional: ?sectorId=<uuid>
}
