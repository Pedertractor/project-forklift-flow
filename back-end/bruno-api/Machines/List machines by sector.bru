meta {
  name: List machines by sector
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/machines?sectorId={{sectorId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  # Requer JWT: leitura para LEADER, SUPPLY_OPERATOR, ADMIN, SUPERVISOR, MANAGER.
  # Filtra maquinas pelo sectorId informado no environment.
}
